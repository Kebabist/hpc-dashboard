{
  "name": "hpc-job-queue-server",
  "version": "1.0.0",
  "description": "REST API for the HPC Job Queue Dashboard — simulates a SLURM-style job scheduler backed by SQLite.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node src/seed.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
